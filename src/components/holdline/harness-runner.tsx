export function HarnessRunner() {
  // Timeline events are now created from live MCP responses in the Holdline store.
  // This component remains only to avoid changing the page layout.
  return null;
}